# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 現在のフェーズ

**フェーズ 1 実装中**（2026-07 時点）。scaffolding（package.json / webpack / jest / Playwright / CI-1）と `src/` の骨格（popup / app シェル〔router / store / guards / views 雛形〕 / options〔API キー保存・既定モデル設定は実装済み〕 / anchoring 中核〔スパイクから移植〕）に加え、`lib/google/`（OAuth + Sheets + Drive。sr-query-builder からコピー流用）・プロジェクト生成（`features/project/`: 12 タブ + Drive フォルダ 4 種）・Popup S1（ログイン / 新規作成 / 既存 ID / 最近のプロジェクト）・CSV エクスポートビルダー・extraction の AI 応答バリデーション（`validateAiOutput`: zod + 値/quote 矛盾 → confidence=low 強制）・extract-data skill のプロンプト管理（`skills/extractData.ts`: プロンプト構築 + 構造化出力スキーマ + 応答パース）・一括抽出の実行計画（`planRun.ts`: document × スキーマのバッチ分割〔全項目 or section 単位をトークン予算で判断〕+ トークン / コスト概算）・`lib/llm/` 一式（sr-query-builder から移植: `LLMProvider` 抽象 + `GeminiProvider`〔nullable union → Gemini 方言変換を追加〕+ `withRetry` + `withLogging`〔プロンプト版数記録を追加〕+ `providerFactory`〔model 必須。gemini / openrouter を `/` の有無で解決〕+ `OpenRouterProvider`〔OpenAI 互換 API。sr-query-builder から移植・2026-07-04。方言変換不要でスキーマをパススルー〕+ 単価表 `pricing.ts` + `modelCatalog.ts`〔単価表 → Gemini / OpenRouter グループの候補カタログ + provider 別キー未設定文言〕）・一括抽出の実行（`executeRun.ts`: planRun の計画を消費してバッチごとに LLM 呼び出し → 応答検証 → quote アンカリング確定 → Evidence 生成。進捗通知と partial_failure〔バッチ失敗 4 種 + 要素破棄〕の記録）・extraction のサービス層配線（リポジトリ 3 種〔`evidenceRepository`: Evidence バッチ追記 / `annotationRepository`: StudyData・ResultsData の upsert + 重複キー検出 + 値列の追加のみ拡張 / `runRepository`: ExtractionRuns 追記〕+ `aiAnnotationRows.ts`〔Evidence → ai annotator 行の転記素材〕+ `lib/llm/apiLogRepository.ts`〔LLMApiLog 行追記〕+ `app/services/extractionService.ts`〔runExtraction: planRun → withRetry(withLogging(provider)) で executeRun → ai 行転記 → ExtractionRuns 追記。extracted_texts の読込 loadDocumentPages は S3 実装が提供するまで注入〕。あわせて `lib/google/sheets.ts` に `appendRows` を追加し、`updateRow` / `getSheetValues` の範囲指定を全列対応へ一般化〔StudyData の動的値列が Z 列 = 26 列を超えうるため〕）・documents 機能コア（`lib/pdf/`〔pdfjs-dist 6.1.200 固定 + worker 同梱。anchor-spike の抽出ロジックを正式化〕+ `features/documents/`: extracted_texts 形式確定〔form feed 区切り。`extractedText.ts` が正典〕・`detectTextStatus`〔実質テキスト閾値 30 字/頁〕・`documentRepository`・`importDocuments`〔コピー → 抽出 → txt 保存 → Documents 追記。ファイル単位の失敗継続〕・`loadDocumentPages`〔extractionService へ注入する実装〕。Drive に `copyFile` / `getFileBinary` を追加）・S3 画面の UI 結線（`lib/google/picker.ts`〔MV3 の remote code 制約により hosted/picker.html + externally_connectable 方式。API キー・プロジェクト番号は設定済み。GitHub Pages（gh-pages ブランチ）へデプロイ済みで、2026-07-03 の実機通し確認（§1）で Picker の動作も確認済み → hosted/README.md〕+ `AppState.documents` スライス + `app/services/documentsService.ts`〔一覧読込 / Picker 取り込み / study_label 保存〕+ documentsView 本実装〔読み込み中・失敗・空・進捗行 2 段階・text_status バッジ・インライン編集〕+ view の `render(state, ctx)` 化〔`views/types.ts` の ViewContext〕+ ルート別 E2E `app-documents.spec.ts`）・オフライン書き込みキュー（`lib/storage/offlineQueue.ts`: tiab-review の offline-queue を共通化して移植〔ペイロード非依存の `createOfflineQueue<T>`。storage.local ≤ 100 件 + IndexedDB あふれ退避 + flush 途中失敗の順序保存〕。**呼び出し側への結線は未実施** — S6 検証画面の Decisions 追記で使う予定）・S4 プロトコル入力画面（`features/protocol/`: パーサ 3 種〔manual / md / docx。docx は mammoth.js を `lib/docx/extractDocxText.ts` で正式配線〕+ `protocolRepository`〔Protocol タブの追記・version 採番・一覧。ProtocolBlocks 関連は除外〕+ `saveProtocol`〔md / docx の抽出テキストを Drive `raw_protocols/` へ退避 → 新 version 追記。**LLM 抽出なし** — extract-protocol skill は持たず S5 の draft-schema が raw text を読むため、framework_type 等の構造化列は空のまま〕+ `AppState.protocol` スライス + `app/services/protocolService.ts`〔一覧読込 / 保存 / 編集モード / 版選択〕+ protocolView 本実装〔新規フォーム / 読み取り専用 + 版切替 / 再入力フォームの 3 モード。空本文は保存不可〕+ ルート別 E2E `app-protocol.spec.ts`）・S5 スキーマデザイン画面（`features/schema/`: draft-schema skill〔`skills/draftSchema.ts`: プロンプト + 構造化出力スキーマ + zod パース。extract-data と同構成〕+ `validateField`〔snake_case・重複・StudyData 固定列衝突・enum 許容値〕+ `schemaRepository`〔SchemaVersions / SchemaFields の追記・採番・読出〕+ `saveSchemaVersion`〔検証 → field_id 採番（既存は維持）→ 新版追記〕+ `presets/outcomeTemplates`〔二値 / 連続アウトカム〕+ `AppState.schema` スライス + `app/services/schemaService.ts`〔ドラフト実行は withRetry(withLogging(provider)) で LLMApiLog + logs/llm へ記録。経過秒は store 管理〕+ schemaView 本実装〔ドラフト前（サンプル 1〜3 本 + モデル入力）/ 生成中 / 表形式エディタ / 確定済み + 版履歴〕+ ルート別 E2E `app-schema.spec.ts`）・S8 検証 UI の共有基盤 + S6 パイロット画面（`features/verification/`: `decisionRepository`〔Decisions 追記 + document 別読出〕+ `cellState`〔判定履歴の畳み込み → セル状態。undo は 1 件取り消しの追記〕+ `cells`〔entity タブ / section・インスタンスグループ / セルモデル〕+ `highlights`〔Evidence quote → 正規化再特定 → `highlightMap` 矩形。exact / normalized は全出現列挙で「他 n 箇所に一致」切替、検索 `searchPages`〕。`lib/pdf/renderPage.ts`〔pdfjs 6.x の canvas パラメータ描画〕+ `app/ui/pdfViewer.ts`〔単一ページ表示・ページ送り / ズーム / テキスト検索・ハイライトオーバーレイ（PDF ユーザー空間 → CSS 変換・クリックでフォームへジャンプ）・描画競合の連番ガード〕+ `app/views/verificationForm.ts`〔純粋 render: 判定チップ / anchor failed の quote 全文 + 本文内検索 / 編集・棄却の値入力〕+ `app/views/verificationPanel.ts`〔S6 / S8 共有のステートフル 2 ペイン。判定の楽観反映 + onDecision 委譲、キーボード a/e/x/n/j/k/z/f（入力フォーカス中は無効）、`renderCachedVerificationPanel` で store 再描画と同一インスタンスを維持〕+ `app/services/pilotService.ts`〔runExtraction(runType='pilot') 配線 / 検証データ束の組み立て（Decisions・StudyData 読出 + Drive PDF → テキスト層）/ 判定永続化（annotator 行 upsert → Decisions 追記。失敗時は offlineQueue 'decisions' へ退避・成功時に再送）〕+ `AppState.pilot` スライス + pilotView 本実装〔未実行（既定選択 = テキスト層あり先頭 3 本 + planRun コスト概算）/ 実行中（進捗バー）/ 完了（partial_failure 内訳 + 再パイロット導線 + 埋め込み検証 UI）〕+ ルート別 E2E `app-pilot.spec.ts`〔最小実 PDF を stub 配信して canvas 描画 → ハイライト → 判定 → Decisions 追記まで実弾検証 + axe〕）・S8 単独の `#/verify` 画面（`ArmStructures` タブ新設〔requirements v0.7: 群構成の確定を 1 行 = 1 arm・版追記で永続化。12 → 13 タブ。旧プロジェクトは書き込み時にタブ自動作成 `sheets.addSheetTab`〕+ `armStructureRepository`〔追記・採番・最新版の畳み込み〕+ `features/verification/armDraft.ts`〔Evidence から arm 一覧をドラフト。名称は name / label フィールドの値のみ採用〕+ `progress.ts`〔進捗チップの判定済み / 総セル集計〕+ サービス層の共通化〔`verificationService.ts`: S6 / S8 共有のデータ束組み立て（armStructure 込み）・判定永続化（オフラインキュー 'decisions' 共有）・群構成確定の永続化。pilotService はこれへ委譲する形にリファクタ〕+ `verifyService.ts`〔Evidence 全読み → document 別に最新 run を選定 → 版別 SchemaFields 解決 → 進捗チップ〕+ `AppState.verify` スライス + verifyView 本実装〔document セレクタ + URL クエリ `?doc=` の双方向同期（`router.docQueryOf` + bootstrap の `syncVerifyRoute`。セレクタ切替も hash 書き換え経由の一本道）。`?entity=` ディープリンクは S9 と同時に実装済み〕+ 群構成の確定カード〔verificationPanel 拡張: 未確定時は arm / outcome タブをディム → AI ドラフトの編集（行追加 / 削除 / 名称）→ 確定で楽観反映 + ArmStructures 追記。パネル共有により `#/pilot` にも同時適用〕+ ルート別 E2E `app-verify.spec.ts` 2 本〔実 PDF 配信 → ハイライト → 承認 → Decisions 追記 / ?doc= 直リンク → 群構成確定 → ArmStructures 追記（タブ作成込み）+ axe。見出し階層（h2 → h4 スキップ）と非アクティブタブのコントラスト違反を検出して修正済み〕）・S7 一括抽出の `#/extract` 画面（`runRepository.readRunDocumentCoverage`〔完了 run の document_ids 和集合 = 抽出済み判定 + 中断 run の残り文献。既定選択「未抽出の全件」と中断バナーの素材〕+ `features/extraction/docProgress.ts`〔executeRun のバッチ進捗 → document 単位の 待機中 / 実行中 / 完了 / 失敗 への畳み込み。`RunProgress` に `failure` フィールドを追加してバッチ失敗をライブ通知〕+ `AppState.extract` スライス + `app/services/extractService.ts`〔対象読込（抽出済みバッジ / 未抽出の既定選択・S6→S5 のモデル引き継ぎ）/ 実行確認ゲート（選択・モデル・API キー検証 → 確認カード）/ runExtraction(runType='full') 配線 / 失敗文献の再試行 = runType='single_document' で当該 1 本のみ再実行（対象行だけ差し替え）。counts の evidenceRows / dataRows も更新〕+ extractView 本実装〔読み込み中 / 失敗 / 未実行（パイロット未実施の警告バナー + チェックリスト + コスト概算 = pilot と同仕様）/ 実行確認カード `role=alertdialog` / 実行中（進捗バー + document 進捗リスト）/ 完了（done / partial_failure 黄バナー + 再試行 + `#/verify` への導線）〕+ ルート別 E2E `app-extract.spec.ts` 2 本〔未実行の既定選択・バッジ・確認ゲート / 実行 → LLM 400 で 1 本失敗 → 再試行成功 → ExtractionRuns 2 回追記まで実弾検証 + axe〕）・S9 ダッシュボードの `#/dashboard` 画面 + `#/verify` の `?entity=` ディープリンク（`features/verification/dashboard.ts`〔document × section の進捗マトリクス + anchor 失敗率（分母 = anchor_status 非 null）+ not_reported 率（分母 = Evidence 総数）への畳み込み。セルモデル基準 = 検証画面の進捗チップと同じ数え方。セクション先頭セルの entity_key をディープリンク先に持つ〕+ verifyService の素材組み立てを `readVerifyTargetMaterials` へ共通化〔S8 一覧と S9 集計で共有。ownDecisions 込み〕+ `AppState.dashboard` スライス + `app/services/dashboardService.ts` + dashboardView 本実装〔読み込み中 / 失敗 / 0 件（`#/extract` への導線）/ 通常 = サマリ + `<table>` マトリクス。セルは `#/verify?doc=&entity=` へのリンク、スキーマにない section・セル 0 件・分母 0 は「—」〕+ `?entity=` ディープリンク〔`router.entityQueryOf` + `VerifyState.deepLinkEntityKey`（bootstrap の syncVerifyRoute が hash から写す）+ verificationPanel の `focusEntity`（該当タブへ切替 + 先頭セルへスクロール・フォーカス。DOM 接続後の microtask で適用し、群構成未確定でロック中のタブ・不明 entity は無視）。`renderCachedVerificationPanel` は focusEntityKey の変化時だけ発火し、ストア再描画でフォーカスを奪わない〕+ ルート別 E2E `app-dashboard.spec.ts` 2 本〔マトリクス + 率の表示 → セルクリックで検証画面へ / ?entity= 直リンクで arm タブ切替 + セルフォーカス（群構成確定済み stub）+ axe〕）・S10 エクスポートの `#/export` 画面（CSV builder 3 種に `documentCount`〔= CSV に行が出た文献数 = `ExportLog.document_count`〕を追加 + `features/export/parseCsv.ts`〔RFC 4180 の読み戻し。プレビュー / 行数集計用〕+ `buildExport.ts`〔3 形式を共通の `BuiltExport` へ正規化: ヘッダ / 先頭 10 行プレビュー / 行数 / 未検証セル数〔study_wide = 確定行の空セル・audit = 判定 0 件セル・results_long = 概念なし null〕/ 除外警告素材〕+ `exportLogRepository`〔ExportLog 追記〕+ `runRepository.readRunAuditInfos`〔run_id / schema_version / started_at の最小読み出し。domain に `RunAuditInfo` を追加し buildAuditCsv の runs 引数も同型化〕+ `AppState.export` スライス + `app/services/exportService.ts`〔素材 7 種の読込 → 最新確定版 SchemaFields で 3 形式を構築 / 生成 = 未検証セル警告ゲート → Drive `exports/` を ensureChildFolder で確保 → `{format}_{YYYYMMDD-HHMMSS}.csv` を uploadTextFile → ExportLog 追記 / 結果カードの Blob ローカル保存 `app/ui/download.ts`〕+ exportView 本実装〔読み込み中 / 失敗 / 通常（形式ラジオ + サマリ + プレビュー + 除外警告 + データ行 0 件は生成不可）/ 警告ダイアログ `role="alertdialog"`（続行 / 中止）/ 生成中 / 失敗 / 生成完了カード。views/placeholder.ts は役目を終え削除〕+ ルート別 E2E `app-export.spec.ts` 2 本〔形式切替 → 生成 → Drive upload + ExportLog 追記の実弾検証 / 未検証セル警告の中止・続行 + axe〕）・app シェルの Sheets 読込 = 進捗カウントの実データ化（`lib/google/sheets.ts` に `getBatchValues`〔values:batchGet。複数範囲を 1 呼び出し〕+ `features/project/progressCounts.ts`〔`readProgressCounts`: 7 範囲〔各タブのキー列 + ExtractionRuns の run_type 列〕→ ProgressCounts へ畳み込み。`ProgressCounts` 型定義もここへ移設し store は re-export〕+ `AppState.home` スライス〔countsLoaded / countsLoading / countsError〕+ `app/services/homeService.ts`〔`loadProgressCounts`: 起動時 + `#/home` 再入場時に読込。読込済み no-op / force 再取得〕+ homeView 3 状態〔読み込み中 / 失敗 + 再読み込み / 通常サマリ〕+ bootstrap 配線〔起動時に 1 回読込でガードのディム判定も実データ化。E2E seam: `__E2E_PRELOADED_STATE__` に counts があれば読込済み扱いで batchGet を抑止〕+ ルート別 E2E `app-home.spec.ts` 2 本〔batchGet stub → サマリ + ディム解除 / 失敗 → 再読み込みで復帰 + axe〕）・S11 Options の既定モデル設定（`lib/storage/settingsStore.ts`〔`settings.defaultModel`。trim 保存・空は未設定へ解除〕+ schemaService の素材読込時に `schema.model` が空のときだけ注入〔入力済みは上書きしない。S6 / S7 は S5 のモデルを引き継ぐため下流全部に効く〕+ `options-smoke.spec.ts` E2E）・モデル選択のプルダウン化 + OpenRouter 対応（2026-07-04。`app/ui/modelSelect.ts`〔共有ウィジェット `createModelSelect`: 単価表の Gemini / OpenRouter optgroup + 「その他（直接入力）」。S5 `#schema-model` / S6 `#pilot-model` / S7 `#extract-model` / Options `#default-model` の 4 箇所で共用。tiab-review のプルダウンと同じ体験〕+ `secretsStore` に OpenRouter キー（`secrets.openRouterApiKey`）+ Options に OpenRouter キー節 + サービス層の `loadApiKey(provider)` 化〔`resolveProviderId(model)` で Gemini / OpenRouter キーを引き分け。未設定文言も provider 別〕+ manifest `host_permissions` に `https://openrouter.ai/*`）・抽出実行の耐中断性 = ExtractionRuns の 2 行プロトコル（2026-07-06。requirements.md §3.2 `ExtractionRuns`: 実行開始時に `status='running'` 行を **Evidence より先に**追記 → 完了時に確定 status 行を同 run_id で追記。中断しても「Evidence の run_id が ExtractionRuns で解決できない」孤児が生まれない。読み手は完了行のみを抽出済みに数える〔`readRunDocumentCoverage` / progressCounts の pilotRuns〕ため中断 run の文献は未抽出に戻り、S7 の既定選択がそのまま再開手段。`#/extract` に中断バナー `#extract-interrupted-warning` + `#/verify` はプロトコル導入前の孤児 Evidence をエラーにせず未抽出扱いで除外）・S6 パイロットの履歴読込と復元（2026-07-07。`runRepository.readPilotRuns`〔`run_type='pilot'` の完了行のみを新しい順で ExtractionRun へパース〕+ `pilotService` に `loadPilotHistory` / `autoLoadLatestPilotRun`〔入場時に最新 run を一度だけ自動読込 = 既存データがあれば結果 + 埋め込み検証 UI を復元し「最初から」にしない〕/ `loadPilotRun`〔Evidence を run で絞り + 版別 SchemaFields を解決 → `loadPilotVerification` へ〕+ `PilotState` に `history` / `historyInitialized` / `loadingRunId` ほか + pilotView の履歴セクション `#pilot-history`〔日時 / モデル / 文献数 / status バッジ。項目クリックで別 run を読込・表示中は無効化 + 「表示中」〕。新規実行は末尾の「新規パイロット」フォーム、完了 run は履歴先頭へ追加。app-pilot.spec.ts に履歴復元シナリオを追加）・RoB テンプレートスキーマの MVP 前倒し（2026-07-07。requirements v0.9: `rob_domain` レベルを P1 → MVP へ確定。`features/schema/presets/robTemplates.ts`〔RoB 2 = D1〜D5 + overall・判定 low/some_concerns/high、ROBINS-I = D1〜D7 + overall・判定 low/moderate/serious/critical/no_information。各「判定 enum + 根拠 text」の 2 項目で、entity_key `rob:d1_randomization` 等を extraction_instruction に明示列挙 = extract-data / draft-schema のプロンプト本文は不変更・版数据え置き。AI ドラフトは RoB を出さずテンプレート挿入が唯一の入口〕+ プリセット挿入の一般化〔`presets/index.ts` の `SchemaPresetKind` / `SCHEMA_PRESETS`、`insertOutcomePreset` → `insertSchemaPreset` へ改名、S5 に `#schema-preset-rob2` / `#schema-preset-robins-i` ボタン追加〕+ 群構成ロックの適用範囲を修正〔`armDraft.isArmDependentLevel`: ディム対象を arm / outcome_result タブのみに限定し、RoB タブは arm 未確定でも検証可。verificationPanel / verificationForm の `tab !== 'study'` 判定 3 箇所を置換〕）・S3 文献取り込みのフォルダ単位対応（2026-07-09。tiab-review の fulltext フォルダを丸ごと取り込めるように、Picker の `driveView` を `setSelectFolderEnabled(true)` でフォルダ選択可にし、選択 doc に `mimeType` を返却〔hosted/picker.html。要 gh-pages 再デプロイ〕+ `picker.ts` の `PickerSelection.mimeType`（旧デプロイ互換で optional）+ `FOLDER_MIME_TYPE` + `drive.ts` の `listFolderPdfs`〔選択フォルダ直下の PDF を `files.list` でページング列挙。`drive.file` スコープでも選択フォルダ配下は列挙可。再帰なし〕+ `documentsService.importFromPicker` にフォルダ展開の 1 段〔フォルダは直下 PDF へ展開し個別ファイルと結合 → `sourceFileId` で重複排除。PDF 0 件・列挙失敗はトースト案内で中断〕。取り込みパイプライン本体〔importDocuments〕は不変更）まで実装済み。ドキュメント一式が正典：

| ドキュメント | 内容 |
|---|---|
| [docs/requirements.md](docs/requirements.md) | 要件定義書 v0.10。データ設計（Sheets 14 タブ、study / document 分離〔Studies 新設・データ行のキーを study_id へ〕、annotator 軸、ArmStructures = 群構成の確定、rob_domain レベル = RoB テンプレート）・機能要件（S1〜S11）・quote アンカリング方式・未決定事項 Q1〜Q10（レビュー済み） |
| [docs/handoff-study-document-model.md](docs/handoff-study-document-model.md) | v0.10（study / document モデル）実装ハンドオフ。3 フェーズ = 3 PR の段階分割 |
| [docs/ui-flow.md](docs/ui-flow.md) | 画面遷移図モック（Popup / メインビュー hash ルーティング / ガード条件） |
| [docs/architecture.md](docs/architecture.md) | `src/` 構成案・ビルド・テスト方針（実装着手時に承認を取る起案） |
| [docs/ui-states.md](docs/ui-states.md) | UI 状態マトリクス（target spec。**スケルトン段階の実装状況は冒頭の drift 注記を参照**） |
| [docs/test-strategy.md](docs/test-strategy.md) | テスト戦略。jest 100% + Playwright の流用構成・E2E seam（worker 解決 / 状態注入）・PDF fixture 2 層運用・フェーズ計画・CI 段階導入 |

次のステップ: **S1〜S10 の実機通し確認は 2026-07-03 に完了**（Selenium 半自動ハーネス `tools/selenium/manualCheck.mjs` の login〜export/offline シーンで実施）。Picker（§1）・キャンセル系エッジ（§1-3 #1〜3）・S4 プロトコル 〜 S10 エクスポート（§2 #1〜#10）+ Sheets / Drive の裏取りまで OK。通し確認中に実機バグ 2 件を発見・修正済み: (1) study レベル `entity_key` の全破棄 → `validateAiOutput` で `-` へ正規化、(2) 検証パネルの判定進捗が不可視 → 判定進捗バーを追加。§1-3 #4（スキャン PDF `no_text_layer` の確認）も 2026-07-03 に完了し、**§1〜§2 の全マニュアルテストが完了**した — [docs/manual-testing.md](docs/manual-testing.md) §3 の結果メモ参照。テストは jest（カバレッジ 100% 強制）+ Playwright E2E + CI-1 / CI-2（E2E ジョブ。2026-07-03 導入）が稼働済みのため、作業原則 7・8 は**有効**。残実装は [docs/remaining-work-plan.md](docs/remaining-work-plan.md) で管理：タスク A（ドキュメント整合）・B（CI-2）・C（S11 既定モデル設定）は 2026-07-03 完了、タスク D（抽出精度ベンチマーク Q8）は実データベンチ（`experiments/extraction-benchmark-real`、2026-07-06 実行・非公開 gitignore）で **`gemini-3.5-flash` を工場出荷既定に確定**（`FACTORY_DEFAULT_MODEL`。事前登録ベンチ `experiments/extraction-benchmark/` は別建てで凍結保持）。残るは**タスク E（リリース準備）**。

**v0.10 study / document モデル（複数報告文書の統合）フェーズ 1・2 実装済み**（フェーズ 1: 2026-07-07 / フェーズ 2: 2026-07-09。[docs/handoff-study-document-model.md](docs/handoff-study-document-model.md)）: study（試験）を抽出・検証・エクスポートの単位、document（PDF）を quote アンカリング・ハイライトの単位へ分離。(1) domain 層 = `Studies` タブ新設（13 → 14 タブ。`domain/study.ts`）+ `Documents` に `study_id` / `document_role` 追加・`study_label` を Studies へ移設 + データ行のキーを `document_id` → `study_id` へ改名（`StudyData` / `ResultsData` / `ArmStructures` / `Decisions`、`ExtractionRuns.study_ids`、`run_type: single_study`、`LLMApiLog.purpose: extract_study`、`ExportLog.study_count`）。**`Evidence` だけは quote の出所文書のため `document_id` を保持したまま `study_id` を併記**。`DOCUMENT_ROLE_ORDER`（article → registration → protocol → abstract → supplement → other）を domain 層に 1 箇所定義。(2) リポジトリ層 = 全層のキー改名一括対応（`readRunStudyCoverage` / `readDecisionsByStudy` / `readArmStructuresByStudy` 等）+ `features/documents/studyRepository.ts` 新設（追記・行内編集・アクティブ study 解決）。抽出は「1 study = 1 文書」のまま動く（`planRun`/`executeRun` は Evidence に `study_id` を書き、UI は document 単位を維持）。(3) グルーピングのドメインロジック = `detectRegistrationId.ts`（NCT / ISRCTN / UMIN / jRCT / JPRN / ChiCTR / EudraCT / ACTRN の正規表現検出。過検出より取りこぼし優先）+ `groupStudies.ts`（統合・分離の意味論 = 文書集合が変わった study を新 study_id で作り直し、旧行は監査残置。統合候補検出 `findMergeCandidates`）+ `importDocuments` に 1 PDF = 1 study 自動生成（Studies → Documents の順で追記）。(4) S3 UI = study 単位グループ表示 + study_label / registration_id / document_role のインライン編集 + 複数 study 選択 → 統合ダイアログ（`role="alertdialog"`。抽出済みデータありは「未抽出に戻る」警告）+ 統合候補バナー（無視は storage.local へ永続化）。`AppState.documents` に studies / 統合選択 / ダイアログ状態を追加。**フェーズ 2（抽出の study 単位化 = 複数文書連結 + document_index）実装済み**（2026-07-09。ブランチ `feat/study-document-model-phase2`。実機テスト前）: (1) `planRun` を study 単位バッチへ（`groupDocumentsByStudy` で studyId ごとに集約 → role 固定順 → 取り込み順で連結順を決定・`PlannedBatch.documentId` → `studyId` + `documentIds` 配列。`no_text_layer` 文書は連結から除外し全文書除外の study はバッチ化しない。トークン予算は study の全文書合計 + 連結見出しぶんで評価）。(2) `skills/extractData.ts` = 複数文書をロール付き区切り `=== Document i/N [role] filename ===` で連結（`ExtractDataDocument`）+ 出力に `document_index` 追加 + 文書間矛盾時は article 優先 + confidence 低下の指示 + **プロンプト版数 1 → 2**。(3) `validateAiOutput` に `document_index` 検証（`documentCount` 引数。quote ありで欠落・範囲外は破棄 = `invalid_document_index`、文書 1 件のみは欠落を 1 へ解決、not_reported は 1 へ帰属）。(4) `executeRun` = study の全文書を document_index 順にロード → 連結プロンプト → `document_index` が指す文書のテキストでアンカリング → `Evidence.document_id` に出所文書を記録（ロードできた文書だけで続行し全滅時のみ load_failed。`BatchFailure`/`RunProgress` のキーを `documentId` → `studyId` へ）。(5) `docProgress.ts` → `studyProgress.ts`（`ExtractStudyRow` / `createStudyProgressTracker`）。(6) サービス層 = `extractService`/`pilotService` を study 単位選択へ（`features/documents/studySelection.ts` の `buildStudySelection`/`documentsForStudies` を共有。既定選択 = 未抽出の全 study / テキスト層あり先頭 3 study、再試行 = `single_study`）。(7) UI = `ExtractState`/`PilotState` を `selectedStudyIds`/`extractedStudyIds`/`interruptedStudyIds`/`studyRows`/`retryingStudyId` へ、extractView/pilotView は study 一覧（配下文書のロール + ファイル名を副次リスト表示）+ study 単位進捗へ。ViewContext は `onToggleStudy`/`onRetryStudy`。**フェーズ 3（検証の複数文書ビューア + ダッシュボード / エクスポートの study 単位化）は未着手**。

## 目的（ゴール）

MIT ライセンスの OSS Chrome 拡張 **sr-data-extraction-plugin**。SR ツール群 3 部作（sr-query-builder → tiab-review → 本拡張）の 3 作目で、SR の**データ抽出工程**を支援する：

1. Drive 上の採用論文 PDF + プロトコルから、AI が抽出スキーマをドラフト
2. AI が各論文から抽出し、各値に **verbatim quote（根拠箇所）** を付与
3. PDF.js ビューア上で根拠箇所をハイライト表示
4. 人間が accept / edit / reject / not_reported で最終判定（全判断の監査証跡）
5. CSV エクスポート（long / wide / audit の 3 形式）

サーバーレス（Sheets = DB、Drive = ファイル実体、BYOK の Gemini API）。詳細は requirements.md。

## 技術スタック

[tiab-review-plugin](tiab-review-plugin/) / [sr-query-builder-plugin](sr-query-builder-plugin/) に準拠：

- Chrome Extension Manifest V3（メインビューは `chrome.tabs.create` で開くフルページ `app.html` + S1 プロジェクト選択ページ `popup.html`〔`default_popup` は持たず、アイコンクリックは service worker の `action.onClicked` が新規タブを開く: プロジェクト選択済み → `app.html` / 未選択 → `popup.html`〕+ Options）
- vanilla TypeScript（UI フレームワーク不使用）+ webpack
- Google OAuth 2.0（`chrome.identity`）+ Sheets / Drive API（スコープは `spreadsheets` + `drive.file` のみ）
- PDF 描画: `pdfjs-dist`（worker は拡張に同梱、CDN 不可）
- LLM: Gemini API + OpenRouter（`LLMProvider` 抽象経由。カスタムモデルの管理 UI は P1）
- テスト: jest（jsdom、カバレッジ 100% 強制）+ Playwright + axe
- Node.js ≥ 18

## 実装時に押さえる設計判断（requirements.md からの要点）

- **データ本体は 2 系統 + annotator 軸**: study レベルの Table 1 的内容は `StudyData`（wide）、arm 別のアウトカム・RoB は `ResultsData`（long）。全データ行に annotator / annotator_type（`ai` / `human_with_ai` / `human_independent` / `consensus`）を持ち、二重独立抽出（Q4）は annotator 行の複数化で表現。AI 根拠は `Evidence`、判定履歴は `Decisions`（追記型）
- **追記型・上書き禁止**: `StudyData` / `ResultsData` の annotator 行のみ上書き可。他タブは追記のみで、変更履歴は `Decisions` で監査
- **entity_level は 3 レベル**（study / arm / outcome_result）。`entity_key` の形式は requirements.md §3.3
- **quote アンカリング**（§5）が技術的中核: 正規化 → exact / normalized / fuzzy / failed の段階マッチ。`anchor_status` を計測対象にする
- **automation bias 対策**: human 行は空セル（未検証）から開始、accept にも 1 操作必須、未検証セル残存時のエクスポート警告
- **著作権**: 学術研究目的のデータ抽出（TDM）は著作権法上の権利制限規定（30 条の 4 等）により適法との整理（確認 UI・記録列・注意書きは持たない）。取り込み画面の注意書きは PDF の外部送信先（LLM API のみ）の説明だけを表示
- 既定 LLM モデル（Q8）: **工場出荷の既定は `gemini-3.5-flash`**（`src/lib/storage/settingsStore.ts` の `FACTORY_DEFAULT_MODEL`。Options 未設定時に S5 初期値へ注入 → S6/S7 へ伝播）。実データ抽出ベンチマーク（`experiments/extraction-benchmark-real/REPORT.md`, 2026-07-06。不眠 SR 実 gold・非公開）で最良の項目正確度（成功 run 72%）だったため採用。事前登録ベンチ（`experiments/extraction-benchmark/`）は別建てで凍結保持

## サブモジュール

| パス | 役割 | 参照すべきドキュメント |
|---|---|---|
| [tiab-review-plugin/](tiab-review-plugin/) | 技術スタック・オフライン同期・判定 UI トンマナ・LLM ベンチマーク運用の参照実装 | [tiab-review-plugin/AGENTS.md](tiab-review-plugin/AGENTS.md) |
| [sr-query-builder-plugin/](sr-query-builder-plugin/) | メインビュー構成・プロトコル入力画面・Sheets/Drive データ設計の参照実装（アルファ配布は zip 運用だが本拡張は踏襲せず Chrome Web Store 限定公開を使う → remaining-work-plan.md タスク E） | [sr-query-builder-plugin/CLAUDE.md](sr-query-builder-plugin/CLAUDE.md) |

サブモジュール内で作業するときは、そのサブモジュールの CLAUDE.md / AGENTS.md を最優先する。OAuth / Sheets / Drive クライアント・オフラインキュー・LLM 抽象は既存 2 拡張からコピー流用する（npm 切り出しは 3 拡張が揃ってから判断。architecture.md §7-3）。

## 作業上の原則（tiab-review-plugin/AGENTS.md より継承）

1. **ブランチ強制**: `main` / `master` / `develop` で直接作業しない。変更前に作業ブランチを切る。
2. **日本語化**: ユーザー向けアーティファクト（計画書・要件書・コミットメッセージ・コード内コメント）は日本語で書く。思考プロセスだけ英語でよい。
3. **既存テスト保護**: 既存テストが落ちたら、まず実装のバグを疑う。テスト側を直す場合は「意図した仕様変更」であることをユーザーに確認する。
4. **ドキュメント同期**: 仕様や機能を変えたら、関連ドキュメント（README、docs/、コメント）も同時に更新する。特に ui-states.md は「実装より先に spec を書く」運用。
5. **機密情報**: API キー・OAuth トークン等はログ／アーティファクト／チャット応答に絶対出さない。トークンをログに出すときは `token.substring(0, 8) + '...'` で省略する。
6. **自動化の限界**: ツール実行が複数回失敗したら執拗に再試行せず、状況を報告する。
7. **テスト通過後の dev ビルド検証**: `npm test` が通ったら、完了報告前に必ず `npm run dev` で webpack の成功を確認する（実装フェーズ開始後）。
8. **UI 変更時は E2E も回す**: 画面・CSS・ルーティングに触れたら `npm run test:e2e` まで通す（実装フェーズ開始後）。
