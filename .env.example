# Google OAuth 2.0 クライアント ID（Chrome 拡張用）。
# webpack ビルド時に src/manifest.json の __OAUTH_CLIENT_ID__ へ埋め込まれる。
OAUTH_CLIENT_ID=

# dev ビルド（未パック拡張）用のクライアント ID。
# 未設定の場合は OAUTH_CLIENT_ID がそのまま使われる。
LOCAL_OAUTH_CLIENT_ID=
